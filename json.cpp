#include "json.h"

#include <iostream>
#include <string>
#include <map>

std::map<std::string, std::string> from_string(std::string &s) {
    std::cout << s << std::endl;
    std::map<std::string, std::string> finalMap = {};


    return finalMap;
}